image: debian:9

pages:
  script:
  - apt-get update -qq && apt-get install -qq make asciidoc
  - cp -a xsf-docs public
  - make -C public html
  artifacts:
    paths:
    - public
  only:
  - xsf-docs
